# Python dependencies for meta_dev.ipynb (align with ai_in_se_cw)
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - flask
      - flask-cors
      - python-dotenv
      - pillow
      - plantuml
      - requests
      - pytest
      - ipython
